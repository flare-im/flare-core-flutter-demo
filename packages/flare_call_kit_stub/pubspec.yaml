name: flare_call_kit
description: Lightweight no-WebRTC call kit surface for the core Flutter SDK example.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.10.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
